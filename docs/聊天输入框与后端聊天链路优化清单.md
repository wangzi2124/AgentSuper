# 前端聊天输入框 & 后端聊天链路 · 优化清单（合并版）

- **项目**：AgentSuper（C:\tool\AgentSuper-main）
- **日期**：2026-08-27
- **证据范围**：`ChatInput.vue`、`stores/multiAgent.ts`、`api/multiAgent.ts`、`api/errors.ts`、`api/fetch.ts`、`backend/app/api/chat.py`、`backend/app/models/schemas.py`、`backend/app/agent/supervisor.py`、`backend/app/agent/bus.py`、`backend/app/agent/stream_events.py`、`backend/app/session/service.py`、`backend/app/session/agent_executor.py`
- **标注**：优先级 P0=紧急 / P1=重要 / P2=改进 / P3=可选；工作量 S≤1人日、M=1–3人日、L>3人日；风险 = 改动回归面

## 摘要统计

| 层 | P0 | P1 | P2 | P3 | 合计 |
|---|---|---|---|---|---|
| 前端输入框（ChatInput.vue） | 0 | 2 | 5 | 2 | 9 |
| 前端 Store / API 层 | 1 | 1 | 5 | 1 | 8 |
| 后端 API 层（chat.py / schemas.py） | 0 | 4 | 5 | 1 | 10 |
| Agent 层（supervisor / bus / stream_events） | 0 | 1 | 4 | 0 | 5 |
| **合计** | **1** | **8** | **19** | **4** | **32** |

---

## 一、前端输入框（ChatInput.vue）

| # | 优先级 | 问题 | 证据（文件:行） | 建议方案 | 工作量 | 风险 |
|---|---|---|---|---|---|---|
| F1 | **P1** | **中文输入法（IME）误发送**：`Enter` 在拼音选词/联想确认时同样触发发送，中文用户高频踩坑 | `ChatInput.vue:26-31` 仅判断 `e.key === 'Enter' && !e.shiftKey`，无 `e.isComposing` 检查 | 加 `if (e.isComposing || e.keyCode === 229) return`，仅在组合结束且 `Enter` 时发送 | S | 低 |
| F2 | **P1** | **无输入长度上限**：textarea 无 `maxlength`，后端 `ChatRequest.message` 也无 `max_length`（见 B1），超长文本直达 LLM | `ChatInput.vue:48-55`；`schemas.py:50` | 前端 `maxlength=50000` + 超限提示；后端 Pydantic `Field(max_length=50000)` 双保险 | S | 低 |
| F3 | P2 | **固定 `rows=3` 不自动增高**：长文本需手动滚动，体验差 | `ChatInput.vue:53 rows="3"`，无动态高度逻辑 | `@input` 监听动态设 `height = scrollHeight`，限制 min 40px / max 200px，`resize:none` 保留 | S | 低 |
| F4 | P2 | **加载中完全禁用输入**：`loading` 期间用户无法起草下一条，只能干等 | `ChatInput.vue:51 :disabled="loading"` | 不禁用 textarea，仅发送按钮置灰/切换为取消按钮；或支持"排队输入"（草稿暂存） | S | 低 |
| F5 | P2 | **`trim()` 过度裁剪**：发送内容被剥掉首尾空白/换行，代码块粘贴尾部换行丢失 | `ChatInput.vue:14 const msg = text.value.trim()` | 仅用 `trim()` 判空，发送 `text.value` 原文（可再 `normalize` 行尾） | S | 低 |
| F6 | P2 | **发送后焦点丢失**：键盘/触屏用户需重新点击输入框 | `ChatInput.vue:13-18` 清空后未 `focus()`（`defineExpose` 有 focus 但需父组件调用） | 发送成功后 `textareaRef.value?.focus()` | S | 低 |
| F7 | P2 | **无字符/Token 计数**：用户无法感知内容是否接近上下文预算 | 无相关实现 | 右下角字数统计；超阈值（如 40K 字符）变色提示"内容过长将截断历史" | S | 低 |
| F8 | P2 | **无拖拽/粘贴文件上传**：`FileUpload.vue` 存在但未在输入框集成，粘贴截图无法直接发送 | `ChatInput.vue` 仅 textarea+按钮 | `@dragover/@drop/@paste` 拦截文件并 emit 给父组件（需后端附件能力配合） | M | 中 |
| F9 | P3 | **无历史消息 ↑/↓ 导航**：重复/近似提问需重新输入 | 无 | 维护"已发送"栈，↑ 恢复上一条、↓ 前进 | M | 低 |
| F10 | P3 | **placeholder 静态**：固定"请输入关于知识库的问题…"，不随路由/agent 变化 | `ChatInput.vue:52` | 按当前 agent/会话状态动态生成 | S | 低 |

---

## 二、前端 Store / API 层

| # | 优先级 | 问题 | 证据（文件:行） | 建议方案 | 工作量 | 风险 |
|---|---|---|---|---|---|---|
| S1 | **P0** | **SSE 无 stall/超时检测**：`getReader()` 死循环等数据，服务端挂起不推也不断时前端无限 loading；连接建立也**未用** `fetchWithTimeout` | `api/multiAgent.ts:25` 裸 `fetch`、`:56-76` 读取循环无超时 | ① 连接用 `fetchWithTimeout`（fetch.ts 已有）；② 记录最近事件时间戳，>60s 无事件 → `abort()` + 按可重试错误处理并提示"连接超时，正在重试" | M | 中 |
| S2 | **P1** | **自动重试幂等性差**：`retryLastMessage` 先删本地 user 消息再重发，若服务端已落库会产生重复轮次 | `multiAgent.ts:249-271` | 重试前 GET 会话核对"该 user 消息是否已存在"；或请求体带 `client_msg_id` 由后端去重（与 B4 联动） | M | 中 |
| S3 | P2 | **`classifyNetworkError` 三处重复**：store、api/multiAgent.ts、api/errors.ts 的 `toApiError` 逻辑几乎一致，`api/errors.ts` 存在但未被聊天链路复用 | `multiAgent.ts:186-195`、`api/multiAgent.ts:6-15`、`api/errors.ts:41-58` | 收敛到 `api/errors.ts` 单一实现，聊天链路改用它；删除两份重复 | S | 低 |
| S4 | P2 | **`liveContent` / `liveMsgId` 死代码**：全项目仅 4 处引用，全是定义与初始化，未在 `return` 暴露、无任何读写逻辑 | `multiAgent.ts:37-38, 75-76` | 删除，或真正实现"流式内容实时展示"（当前 `agent_stream` 已增量更新 agents，但主 content 到 `done` 才回填） | S | 低 |
| S5 | P2 | **`persistSession` 无防抖**：SSE 每事件、每错误、每 done 都可能触发 IndexedDB 写入，频繁 I/O | `multiAgent.ts:332, 407, 437, 462` 等多处直接 `await persistSession` | 1–2s 防抖合并写，页面/组件卸载前 `flush` 一次；同一会话并发写加队列串行 | S | 低 |
| S6 | P2 | **发送前无模型/目录有效性校验**：`selectedModel` 是否可用、`sessionDirectory` 是否仍是有效工作区均未检查，后端 404 才报错 | `multiAgent.ts:334` 直接拼 `reqData` | 发送前用配置列表校验 model；目录经后端 `discover_project_root` 语义兜底并给出前置提示 | S | 低 |
| S7 | P2 | **排队时多次点击发送被静默丢弃**：`session.loading` 直接 return，用户无感知 | `multiAgent.ts:309` | 返回 toast/内联提示"上一条仍在处理中"；或支持排队队列（一次一条） | S | 低 |
| S8 | P3 | **自动重试计数绕路**：用"连续 error 消息数"间接计数（`consecutiveErrors`），与手动重试/撤销后的消息序列耦合，语义脆弱 | `multiAgent.ts:214-218` | 改为独立 `retryCount` 计数器，在 `send` 成功/`done`/`cancel` 时清零 | S | 低 |

> 补充说明（非问题）：`api/multiAgent.ts:66-74` 只消费 `data:` 行、忽略其它行——这天然兼容后端新增的 SSE 注释心跳（`:` 开头），后端加心跳无需前端改动。

---

## 三、后端 API 层（chat.py / schemas.py）

| # | 优先级 | 问题 | 证据（文件:行） | 建议方案 | 工作量 | 风险 |
|---|---|---|---|---|---|---|
| B1 | **P1** | **`ChatRequest.message` 零校验**：裸 `str`，无 `min_length/max_length`，空串与超长文本直达 supervisor → LLM | `schemas.py:50` | `Field(min_length=1, max_length=50000)` + `str_strip_whitespace`；超长返回 422 | S | 低 |
| B2 | **P1** | **非流式端点错误信息裸透传**：`detail=f"Agent error: {e}"` 直接把异常字符串（可能含内部路径/堆栈）返回前端；流式端点虽已用 `classify_error`（:515）但 `error/detail` 仍带 `str(e)` | `chat.py:325`（非流式）、`chat.py:513-515`（流式） | 对外统一模糊化 message（"Agent 执行失败，请稍后重试"），完整异常写日志；非流式端点也走 `classify_error` | S | 低 |
| B3 | **P1** | **历史全量拉取**：`service.messages()` 未传 `after_seq/limit`，长会话 O(n) 全量拉取 + 逐条 `estimate_tokens` + `list_parts_for_messages` 全量 parts，虽然后续被截断，但拉取/投影本身浪费 | `chat.py:192-194`；`service.py:150-152`（已支持 limit） | ① 先取最近 N 条（如 200）+ 时间倒序截断；② parts 按消息 ID 分批 IN 查询；③ `estimate_tokens` 只在截断阶段计算 | S | 低 |
| B4 | **P1** | **重试幂等/消息去重缺失**：无 `client_msg_id` 或"消息已存在"探测，前端自动重试（S2）与服务端落库可能产生重复轮次 | `chat.py` 落库逻辑（`_persist_multi_agent`） | 请求带可选 `client_msg_id`，后端落库前去重；或提供"查询消息是否存在"端点供前端重试前核对 | M | 中 |
| B5 | **P1** | **SSE 无 keep-alive 心跳**：supervisor 长时间思考（LLM 分解 + 子任务）期间零推送，Nginx/网关默认 60s 超时掐断连接 | `chat.py:532-550` event_generator 只消费事件队列 | 每 15–30s 推 SSE 注释行 `: ping`（或 `{type:'ping'}` 事件）；前端已忽略非 `data:` 行 | S | 中 |
| B6 | **P1** | **路由关键词硬编码中文词表**：`_decompose` 快速路径靠中文关键词 + ≤24 字符判定走 RAG，英文问题、模糊/跨领域问题误判（如"查一下今天的股票行情"不在任何词表） | `supervisor.py:267-289` | 混合路由：关键词 + 模型快速分类 + 置信度阈值；补充英文词表；低置信度一律走 LLM 分解 | M | 中 |
| B7 | P2 | **排队无上限**：`_queue_counter` 仅统计排队数，无 `MAX_QUEUE_SIZE`；极端并发下排队请求无限堆积（每请求占一个 SSE 连接 + asyncio task）；且递减逻辑需复核 | `chat.py:46`（`_queue_counter` 语义） | 加 `MAX_QUEUE_SIZE`（如 50），超限直接 429；修正计数增/减配对 | S | 低 |
| B8 | P2 | **`_generate_title` 未清洗**：直接取用户消息前 20 字符存库，含控制字符/潜在注入面（Vue 转义缓解了 XSS，但导出/其它渲染路径仍有风险） | `chat.py:95-102` | `html.escape` + 去除控制字符 + 字节安全截断 | S | 低 |
| B9 | P2 | **历史注入与 graph 上下文预算无联动**：`MAX_HISTORY_TOKENS=16_000` 硬编码，graph 侧 `max_context_tokens` 24K（usable≈15.8K），16K 注入已逼近可用预算 | `chat.py:59`、`:57-58` 注释 | 由 `settings.max_context_tokens` 动态推导 `history_budget`（如 40–50%），并预留安全余量（90% 生效）；摘要 `keep` 参数联动 | M | 低 |
| B10 | P2 | **摘要/截断双路径不一致**：有 summarizer 走 `summarizer.apply`，否则走 `_truncate_history`，两处调用点（:288-290 / :384-386）保留策略不同，同一会话不同请求历史注入不一致 | `chat.py:288-290, 384-386` | 抽公共函数统一管线：截断 → 可选摘要 → 占位标记，两处共用 | M | 中 |
| B11 | P2 | **客户端断连不落库部分结果**：`event_generator` 的 `finally` 只 `task.cancel()`，不保存 `collector.agents_snapshot()` 与已生成部分文本，中断后本轮内容全丢 | `chat.py` finally 分支 | 断连/中断时写 `status="interrupted"` + agents 快照 + 部分 content，下次加载可恢复 | M | 中 |
| B12 | P2 | **`X-User-Id` 无签名校验**：直接信任前端头，无来源 IP 审计 | `chat.py`（`_get_user_id`） | 至少记录 (user_id, 来源 IP) 审计日志；或升级为会话令牌 | S | 低 |
| B13 | P3 | **主/子会话双写非原子**：`_persist_multi_agent` 中主会话与子会话分别持锁写入，中途崩溃可能一侧有 user 消息、另一侧没有 | `chat.py`（`_persist_multi_agent`） | 事务化/两阶段提交，或先写主会话再幂等写子会话并允许补偿 | M | 中 |

---

## 四、Agent 层（supervisor.py / bus.py / stream_events.py）

| # | 优先级 | 问题 | 证据（文件:行） | 建议方案 | 工作量 | 风险 |
|---|---|---|---|---|---|---|
| A1 | P2 | **LLM 分解 `max_tokens=512` 偏小**：拆 3 子任务的 JSON 输出易截断 → 解析失败 → few-shot 重试 → 额外一次 LLM 调用（延迟与成本翻倍） | `supervisor.py:330` | 提到 768–1024；解析失败时给模型喂截断提示而非整体重试 | S | 低 |
| A2 | P2 | **Supervisor 无 `touch` 心跳上报**：`bus.send_and_wait` 只能感知子 Agent 的 `touch`（`bus.py:66`），supervisor 自身的 LLM 分解/等待期对上层不可见，导致 `grace_extensions` 式宽限 hack | `bus.py:66`；supervisor 思考路径 | supervisor 在分解/等待阶段周期 `touch`；或对 supervisor 单独配置超时策略 | M | 中 |
| A3 | P2 | **`bus._pending` 无过期清理**：若 `send_and_wait` 调用方被 GC 且未走 `finally`，`_pending[thread_id]` 的 Future 永久残留（thread_id 唯一不可复用，内存泄漏） | `bus.py:31` | Future done 后即从 `_pending` 移除；定时扫描超龄条目（如 10 分钟无引用则清理） | S | 中 |\n| A4 | P2 | **`AgentEventCollector.put_nowait` 无背压**：`events` 列表无限增长，超长工具循环（`tool_output` 被丢弃但步骤事件仍累积）内存线性上涨 | `stream_events.py` | `events` 列表设上限（如最近 500 条），溢出时压缩为\"已折叠 N 步\"占位 | S | 低 |\n| A5 | P3 | **`TaggedEventQueue` 丢弃 `tool_output`/`tool_heartbeat`**（现有设计合理，保留）—— 但需确认丢弃策略覆盖所有高频事件类型，避免其它高频事件漏网 | `stream_events.py` | 审计事件类型清单，形成显式 allowlist/drop 列表并注释 | S | 低 |\n\n---\n\n## 五、跨层联动与落地路线图\n\n### 5.1 关键联动关系（修复时必须成对做）\n\n| 联动组 | 前端 | 后端 | 说明 |\n|---|---|---|---|\n| 输入长度 | F2 maxlength + 提示 | B1 `Field(max_length)` | 缺一不可：单靠前端可被绕过，单靠后端体验差 |\n| 重试幂等 | S2 重试前核对 | B4 `client_msg_id` 去重 | 两端同时改才杜绝重复轮次 |\n| SSE 保活 | S1 stall 检测（60s 无事件 abort） | B5 心跳（15–30s 注释行） | 心跳使 stall 检测真正有效（正常空闲与故障可区分） |\n| 历史预算 | — | B9 动态 history_budget | 配合 graph 的 `max_context_tokens`，改一处常量全链路生效 |\n| 错误分类 | S3 收敛到 `api/errors.ts` | B2 走 `classify_error` | 前端 ChatError 与后端 error_type/retryable 字段一一对应 |\n\n### 5.2 建议落地顺序（按投入产出比）\n\n**第一批 · 防御性修复（1–2 天，风险最低）**\n1. B1 消息校验 + B2 错误脱敏（后端 30 分钟）\n2. F1 IME 误发送 + F2 maxlength（前端 30 分钟）\n3. B3 历史 limit 拉取（前端无需改，后端 1 小时）\n\n**第二批 · 可靠性（2–3 天）**\n4. S1 SSE stall 检测 + B5 心跳（联动组）\n5. B7 排队上限 429 + B8 标题清洗 + B12 审计日志\n6. B6 路由关键词增强（含英文词表）\n\n**第三批 · 幂等与体验（3–4 天）**\n7. S2 + B4 重试幂等（联动组）\n8. F3–F6 输入框体验四项（自动增高/非禁用/trim/focus）\n9. B9 动态 history_budget + B10 统一历史管线\n\n**第四批 · 架构打磨（可排期）**\n10. S3 错误分类收敛 + B11 断连部分落库 + A1–A4 Agent 层\n11. F7–F10 / S5–S8 / B13 / A5 低优先项\n\n### 5.3 验收标准（每条落地后验证）\n- **B1/F2**：发送 50K+ 字符被前端拦截并提示；curl 直发超长 body 返回 422。\n- **S1/B5**：模拟后端 90s 无事件（改代码或断点），前端 60s 后出现\"连接超时，正在重试\"且自动重发成功。\n- **B3**：2000 轮历史会话，`/chat` 首包延迟与后端历史拉取耗时显著下降（可加日志对比）。\n- **S2/B4**：强制断网后自动重试，服务端会话无重复 user/assistant 轮次。\n- **F1**：中文输入法下用 Enter 选词不触发发送，仅确认后 Enter 发送。\n\n---\n\n## 六、证据文件清单（已核实）\n\n| 文件 | 关键行 | 核实结论 |\n|---|---|---|\n| `frontend/src/components/ChatInput.vue` | 13-18, 26-31, 48-55 | 全量阅读 144 行；确认 trim 裁剪、IME 无防护、固定 rows、loading 禁用 |\n| `frontend/src/stores/multiAgent.ts` | 37-38, 186-195, 214-218, 249-271, 298-475 | 全量阅读 564 行；确认死代码、重复分类器、重试逻辑、persist 无防抖 |\n| `frontend/src/api/multiAgent.ts` | 6-15, 25, 56-76 | 全量阅读 84 行；确认裸 fetch、无 stall 检测、data: 行解析 |\n| `frontend/src/api/errors.ts` | 41-58 | 全量阅读 99 行；确认 toApiError 与 store 分类器重复 |\n| `backend/app/api/chat.py` | 46, 59, 95-102, 192-194, 288-290, 325, 384-386, 513-517, 532-550 | 全量阅读 558 行；确认排队计数、历史全量拉取、双路径、错误透传、无心跳 |\n| `backend/app/models/schemas.py` | 50 | 确认 ChatRequest.message 裸 str 无校验 |\n| `backend/app/agent/supervisor.py` | 267-289, 330 | 确认关键词路由、max_tokens=512 |\n| `backend/app/agent/bus.py` | 31, 66 | 确认 touch 机制、_pending 无清理 |\n| `backend/app/agent/stream_events.py` | 全量 | 确认事件收集/快照/失败兜底、无背压 |\n| `backend/app/session/service.py` | 150-152 | 确认 messages() 支持 after_seq/limit 但 chat.py 未传 |\n| `backend/app/session/agent_executor.py` | 18 | 确认 classify_error 存在且流式端点已用、非流式未用 |\n\n---\n\n*文档生成于 2026-08-27，基于代码静态阅读，所有行号为当日代码版本。未读文件：`backend/app/agent/graph.py`（80K，上下文管理细节）、`backend/app/middleware/summarization.py`（摘要触发阈值）、`backend/app/session/repository.py`（write_lock 实现）——涉及 B9/B10/B13 的最终方案需补充阅读后再动工。*\n
---

## 七、实施与验证状态（2026-08-27）

| # | 状态 | 说明 |
|---|------|------|
| B1 | ✅ 已实施 + 已验证 | v2 写法 `Annotated[str, StringConstraints(strip_whitespace=True, min_length=1, max_length=50_000)]`（`schemas.py:50`）；实测：空串/50001 超长/纯空白→422，50000 边界 OK（len=50000），首尾空白自动去除（`"  hello  "`→`"hello"`） |
| B2 | ✅ 已实施 | 错误响应统一模糊化（`chat.py:344,351,470,536`） |
| B3 | ✅ 已实施 | 历史 limit=200 拉取（`chat.py:187-204`） |
| B5 | ✅ 已实施 | SSE 心跳每 20s `_ping`（`chat.py` `event_generator()`） |
| B7 | ✅ 已实施 | `MAX_QUEUE_SIZE=50` + 排队满 429（`chat.py:46,426-435`） |
| B8 | ✅ 已实施 | `_generate_title` 去控制字符 + `html.escape`（`chat.py:98-108`） |
| B9 | ✅ 已实施 + 已验证 | `MAX_HISTORY_TOKENS=16_000` 与 `usable_context_tokens()=151_808` 联动断言 PASS |
| B12 | ✅ 已实施 | `_get_user_id` 审计日志（`chat.py:38-48`） |
| F1 | ✅ 已实施 | IME 合成期间 Enter 不触发发送（`ChatInput.vue:38-39`） |
| F2 | ✅ 已实施 | `maxlength=50000` 前端拦截（`ChatInput.vue:13,65`） |
| F3 | ✅ 已实施 | textarea 自动增高 80–200px（`ChatInput.vue:25-29,69`） |
| F5 | ✅ 已实施 | trim() 过度裁剪修复，发送原文（`ChatInput.vue:36-39`） |
| F6 | ✅ 已实施 | 发送后焦点恢复（`ChatInput.vue:42-43`） |
| F7 | ✅ 已实施 | 字符计数器（<5000 显示，<1000 红色）（`ChatInput.vue:16-19,70-74`） |
| S1 | ✅ 已实施 | SSE stall/超时检测（`multiAgent.ts:17-33,56-100`） |
| B4/B6/B10/B11/B13、S2-S8、A1-A5、F4/F8-F10 | ⏳ 未实施 | 见 `patch/CHANGELOG.md`「三、未实施项（待后续批次）」 |
